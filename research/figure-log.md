# 図版ログ(採用記録と判断メモ)

採用した図版について「なぜその構図にしたか」「プリセットからどう逸脱したか」を残す台帳。
**プロンプト全文は書かない**。全文は各 md の `::: details` 内に採用版が入っており、二重に持つとズレるため。

形式・運用ルールは `research/writing-guide.md` の「図版プレースホルダー」節を参照。

## 全図に効く教訓

図を作るたびに踏んだ罠をここに足していく。次の図を作る前に読む。

- **線の接続先は「どこから」を具体的に書く**。「3 つのボックスの中間から下へ」と書いたら、生成側が「真ん中のボックス(Part II)から」と解釈した(図 0-1 の 2 版目)。全体から 1 本ぶら下げる構図は誤解されやすい。**繋がない方が正確に伝わるならそもそも線を引かない**
- **線の禁止は 2 箇所に書く**。生成モデルは線を勝手に補完する。LAYOUT に `no connecting line` と書き、さらに ARROWS の末尾に `No other lines or connectors anywhere in the diagram.` を足すと通る
- **特定の要素だけを青で塗らない**。「1 つだけ青」は視覚的に「これが主役/ゴール」と読まれる。並列な要素は同じ塗りにし、青は矢印など「進行」に使う(CLAUDE.md の中立性ルールと直結する)
- **横一列に 6 要素以上を 3:2 に入れない**。文字が小さくなる。ワイド 2:1 プリセットを使う
- **同じ文字列を図に 2 回出してよい**。プリセットの `Render every quoted label verbatim, exactly once` と衝突しそうだが、左右のパネルに同じ `your code` を 1 つずつ置く指定は通った。**対比図では起点を共有せず各パネルに複製する**方が、パネルをまたぐ矢印の交差を避けられる(図 9-3)
- **前後を比べる図は、変化を矢印で描かず `before` / `after` のラベルで示す**。「行き先が図の外の空間」になる矢印は位置が定まらない。左右が時間の前後だと分かれば、変化そのものは絵が語る(図 5-2)
- **ドット始まりのファイル名・長いパスはピリオドの個数を指定する**。`node_modules/.pnpm` が `node_modules/..pnpm` になった(生成モデルが先行ドットを省略記号と誤認する)。`with exactly one period before the letters "pnpm" and no other periods` のように**数で指定**すると通る。ディレクトリ名は読者が手元で `ls` する対象なので見逃さない(図 9-1)
- **アイコンを使う図には `ICON STYLE:` を明示する**。プリセットの `simple geometric icons` だけでは弱く、カラーの 3D イラスト(グラデ付きの立方体、塗り絵のビールジョッキ)が出ることがある。DIAGRAM CONTENT に `every icon is a simple line-art outline drawing ... no fill, no color, no gradient, and no 3D shading` を足すと線画に揃う。線の禁止を 2 箇所に書くのと同じ考え方(図 8-1)
- **装飾アイコン(警告三角形・稲妻)より言葉を置く**。「異常が起きた」ことは枠の色とタグで伝わる。図が足すべきは異常の**中身**(`not declared` / `same package.json`)。アイコンは要素が増えるだけで、プリセットの `simple geometric icons` からも外れる(図 3-3・4-1)
- **塗りは階層を、枠の色は状態を表す**。同じ階層の要素を 1 つだけベタ塗りにすると、階層が違うように見える。白背景+太い色枠なら「同格だが状態が違う」を表現できる(図 3-2 で確立。図 2-1 の MAJOR/MINOR も同じ形)

## 採用記録

### 図 0-1 この本の構成(2026-08-30 / 標準 3:2)

`docs/introduction.md` — Part I/II/III の 3 ボックス + Appendix。

初版は `Box 3 (blue, gem icon) labeled "pnpm"` で、pnpm だけが青塗り+宝石アイコンだった。「Basics → History を経てたどり着く宝物が pnpm」と読め、CLAUDE.md の「pnpm はゴールではなく最新世代の題材」に反するため差し替えた。3 部を同格の light gray にし、青は矢印へ。宝石は積層ディスク(store の構造を示唆)に変更。

2 版目は Appendix を縦線で吊るしたが、線が Part II の底から出て「History の付録」に見えたため、3 版目で線を撤去し 2 段の独立レイアウトにした。

章番号(`Ch 1-4` など)は 1 ボックス 3 行になり組みが崩れやすいので入れていない。直後の Part 表が対応を示すので図には不要と判断。

### 図 1-1 4 つの登場人物(2026-08-30 / 標準 3:2)

`docs/basics/01-what-is-a-package-manager.md` — Developer / Package Manager / Registry / node_modules。

プロンプトは初版のまま変更なし。**Package Manager だけが青**だが、これは特定ツールの推しではなく「本書の主役は CLI ツール本体」を示すもので、本文の記述とも一致するため中立性の問題はない。

初版の生成結果は矢印が斜めに交差し、write 矢印が PM の枠から離れていた。再生成で直角配線になり、3 本すべてが PM を起点/終点として繋がる形になったので後者を採用。人物アイコンの描き込みも減り `simple geometric icons` に寄った。

node_modules だけ light gray の箱に入っていて 4 要素の視覚的な重みが揃っていないが、プロンプト指定どおりであり実害がないためそのまま採用。

### 図 1-2 4 つの仕事のパイプライン(2026-08-30 / ワイド 2:1)

`docs/basics/01-what-is-a-package-manager.md` — package.json → Resolve/Fetch/Link/Run Scripts → node_modules。

初版プロンプトは `Box 1 (blue, ...)` で Resolve だけが青塗りだった。4 工程は並列に対等なので、1 つ目だけ強調すると「Resolve が主役」と読める。0-1 と同じ構図の問題として同格の light gray に統一し、青は矢印へ。

工程番号 `1`〜`4` を各ボックスに入れた。本文が「解決・取得・配置・実行」の 4 つを順に節で解説するため、図の番号と本文の順序を対応させて行き来できるようにする狙い。

**本書で最初のワイド 2:1**。6 要素を横一列に並べるため 3:2 では文字が小さくなる。比率指定だけだと中央に小さく描かれることがあるので、LAYOUT に `spanning the full width of the canvas with even margins` を併記した。生成結果は 1774x887 でちょうど 2:1。

### 図 1-3 依存の依存がツリーに広がる(2026-08-30 / 標準 3:2)

`docs/basics/01-what-is-a-package-manager.md` — your-app → express → body-parser/debug/send → ms。

**your-app だけが青**だが、これは「あなたが書く 1 行」を指すもので中立性の問題はない。青い矢印 1 本と黒い矢印 4 本の対比が、本文の「1 行の宣言が数十個のインストールを意味する」を視覚で支える。

初版プロンプトは `declared` / `transitive` の浮遊タグ 2 個を置いていたが、どの要素を指すか曖昧で配置も不安定なため、矢印ラベル `you write this` 1 個に集約した。抽象語より「あなたが書くのはここだけ」の方が主張に直結する。`transitive` 側は矢印の色の対比で十分に読めるのでラベルを置いていない。

`ms` の枠を orange から light gray に変更。**オレンジは「問題・注意」の色**であり、ここは正常な推移的依存で警告ではない。

ツリーは階層がずれやすいので、LAYOUT で 4 列構成と各列のノード数・整列(ms は debug と同じ高さ)を明示した。

### 図 2-1 semver の 3 つの数字(2026-08-30 / 標準 3:2)

`docs/basics/02-package-json-and-semver.md` — 1.2.3 の各桁と MAJOR/MINOR/PATCH の対応。

**色分けは初版のまま残した**。オレンジ=MAJOR は「互換性を壊す=注意」で、オレンジを警告色として使う原則に合致する。青=MINOR、墨=PATCH と影響度の大きさが色の強さに対応していて理にかなっている。

変更点は 3 つ。`callout line`(斜め可)を各桁の真下への垂直線に変更し、箱を対応する桁の真下に中央揃えするよう明示した(0-1 で線の起点を曖昧にして失敗した反省)。また初版が使っていた `navy` はプリセット外の色なので `near-black` に修正。

生成結果は線が各桁と同じ色になった(指定外)が、対応関係が強まるのでそのまま採用。桁と箱の水平位置がわずかにずれているものの、垂直線があるので対応は明確に読める。

### 図 2-2 ^ と ~ が許す範囲(2026-08-30 / 標準 3:2)

`docs/basics/02-package-json-and-semver.md` — ^1.2.3 と ~1.2.3 の範囲を数直線で対比。

初版プロンプトは**下の行だけ目盛りを 2 つ**にしていた(1.2.3 と 1.3.0)。これだとバーが短いのか物差しが短いのか区別できず、比較図として機能しない。両行に同じ 3 目盛りを同じ位置で置かせ、**同一スケール上でバーの長さを比べられる**ようにした。

バーの終点は「〜の手前まで」という相対表現ではなく、「中央目盛りを覆う」「中央目盛りの手前で止まる」と**見た目の結果**で書くと生成が安定する。

オレンジの X は「そこには行かない=境界」を示すもので、警告色の使い方として妥当なので初版のまま残した。

### 図 2-3 dependencies の種類マップ(2026-08-30 / 標準 3:2)

`docs/basics/02-package-json-and-semver.md` — 4 種の dependencies を 2x2 で並べる。

初版の descriptor は `runtime` / `dev only` だったが、これは「実行時か開発時か」という軸に読める。**本文の区別軸は「誰の環境に入るか」**(利用者にも入る/自分の環境だけ)なので、`ships to users` / `your machine only` の対になる表現に変えて本文と揃えた。

アイコンを 4 つとも指定した(配送箱・レンチ・握手・疑問符)。2x2 の文字だけだと 4 つが均質に見えて区別が付きにくいため。二重支持の原則。

`dependencies` だけが青だが、これは 4 つの中で最も基本かつ頻度が高いためで中立性の問題はない。オレンジ枠は optionalDependencies の「失敗しうる」に対応していて警告色の使い方として妥当なので初版のまま残した。

懸念していた `optionalDependencies`(20 文字)は 1 行に収まった。

### 図 3-1 npm v2 と v3 の配置の対比(2026-08-30 / 標準 3:2)

`docs/basics/03-node-modules.md` — ネスト構造とフラット構造を左右で対比。

初版プロンプトは左右で名前がバラバラだった(右のルートが `flat`、3 箱が `A2` `B2` `C2`、左の重複が `C` と `C copy`)。これでは**別物の比較**に見えて、「同じ A・B・C が配置だけ変わる」という主眼が消える。左右とも `node_modules`、箱はすべて `A` `B` `C` に統一した。`flat` も `C copy` も実在しないフォルダ名で、その意味でも不適切だった。

オレンジは v2 側の 2 つの C にのみ。重複=解決すべき問題を示す。v3 側は解消後なので色を付けない。

なお、この図は D2(diagram-as-code)でも試作したが、左右パネルの高さが揃わず ELK/dagre のレイアウト調整に手間取った。ChatGPT 生成は一発で揃った。

### 図 3-2 hoisting とバージョン衝突(2026-08-30 / 標準 3:2)

`docs/basics/03-node-modules.md` — C v1 が hoist され、C v2 が B の下にネストする配置。

**矢印を落とした**。初版は `needs v1` / `needs v2` の 2 本を引いていたが、この図が答えるべき問いは「衝突したときどこに置かれるか」であって「誰が要求したか」ではない。依存グラフは直前の Mermaid が示しているので、この図は配置結果に専念させる。矢印を入れるとネストした C v2 から出る線が箱をまたいで交差し、構造が読めなくなる。

**1 回目の生成は塗りで作ったが差し替えた。**C v1 を青ベタ塗りにすると、A・B と同じ階層のはずなのに視覚的な重みが突出し、「C v1 だけ別物」に見えた。白背景+太い青枠に変え、C v2 も白背景+太いオレンジ枠にしたことで、3 つの箱が同格に見えつつ状態の違いが読める形になった。

タグは箱の上辺に食い込む形で生成されたが、浮遊させるよりどの箱に属するかが明確で良い。

### 図 3-3 幽霊依存(2026-08-30 / 標準 3:2)

`docs/basics/03-node-modules.md` — package.json は express しか宣言していないのに、app.js が debug を import できてしまう構図。

**警告三角形をやめた**。初版は debug の隣にオレンジの警告三角形を置いていたが、異常さは太いオレンジ枠と `not declared` タグで十分伝わる。三角形は要素が 1 つ増えるだけで、プリセットの `simple geometric icons` からも外れる。

`declares express` のタグを矢印ラベル `declares` に統合した。タグと矢印で同じ情報が二重になっていたため。

矢印は 2 本で色を分けた。`import debug` を青にしたのは、これがこの図で読ませたい主役の動線だから。`declares`(黒)との対比で「宣言した経路」と「宣言していない経路」が読める。

express の塗りも青ベタから白+細枠に変更(debug と同じ階層なので塗りを揃える。図 3-2 で確立した規則)。

なお下書き段階で A・B・C の抽象名で書きかけたが、**本文は express / debug という具体名で説明している**ため既存プロンプトの具体名を維持した。本文を確認せずに図を設計すると起きるズレ。

### 図 4-1 lockfile なしのズレ(2026-08-30 / 標準 3:2)

`docs/basics/04-lockfiles.md` — 同じ package.json から 1 月と 3 月で違うバージョンが入る。

**稲妻アイコン+`mismatch` を、テキスト `same package.json` に置き換えた**。食い違っていることは 1.2.3 と 1.3.0 という数字で既に見えている。図が足すべき情報は「**同じ package.json なのに**違う結果になった」という理不尽さで、それがこの章の問題提起そのもの。稲妻は「何か悪いことが起きた」以上を語らない。

オレンジ枠は Developer B 側だけ。後から入った人が違うものを掴む、という時間差の被害者を示す。

矢印ラベルは `install January` から `January` へ短縮(`install` は矢印の向きから自明)。

### 図 4-2 lockfile による再現性(2026-08-30 / 標準 3:2)

`docs/basics/04-lockfiles.md` — 同じ lockfile から local と CI が同じ結果を得る。

**4-1 と対になる構図**にした。4-1 はオレンジ枠で 1.2.3 と 1.3.0 が食い違い、4-2 は青枠で両方 1.2.3 が一致する。連続する 2 図なので、構図を揃えると「ロックファイルが入ると何が変わるか」が図同士の差分で読める。

初版の右端のブラケット(2 つの folder を繋ぐ縦の括弧)+ `identical` タグを、テキスト `same result` に置き換えた。装飾より言葉を置く教訓に従い、かつブラケットは 0-1 の縦線と同じく位置が不安定になりやすい。

箱のラベルは `node_modules` / `same tree` から両方 `1.2.3` へ。左右で名前が違うと別物に見える(3-1 の教訓)。**同じ結果になる**ことが主張なので同じ文字列を置く。

なお下書き段階で 4-1 の「1 月と 3 月」の構図を流用しかけたが、**本文のこの節の対比軸は local と CI**。3-3 に続いて本文未確認による設計ミスをしかけた。図を設計する前に必ず本文を読む。

生成結果は lockfile 側の矢印が 1 本から分岐する形になった(指定は 2 本)が、配線が整理されているのでそのまま採用。

### 図 5-1 npm の年表(2026-08-30 / ワイド 2:1)

`docs/history/05-npm.md` — 2010 誕生 / 2015 v3 / 2016 left-pad / 2017 v5 / 2020 GitHub。

5 つの節目は**本文と照合済み**(v3=2015、v5=2017、left-pad=2016 3 月、GitHub 買収=2020)。年表は数字が多く化けやすいので、生成後に年号の並びを必ず確認する。今回は並べ替えも重複も起きなかった。

ラベルを 1 行(`2010 npm born`)から 2 行(`2010` / `npm born`)に変更。年とイベントが繋がって読みにくいため。図 1-2 の番号+名前と同じ形。

上下交互配置は「alternate」とだけ書かず、**1 番目=上、2 番目=下…と個別に指定**した(0-1 で曖昧な指示が誤解された反省)。

プリセット外の `dark navy` を `dark` に修正(2-1 の navy と同じ)。

オレンジは left-pad のみ。他が青の中で 2016 年だけ色が違うのは「事件」だからで、警告色の使い方として妥当。

年表は横長が自然なのでワイド 2:1。生成結果も 1774x887 でちょうど 2:1。

### 図 5-2 left-pad 事件(2026-08-30 / 標準 3:2)

`docs/history/05-npm.md` — 最下段を抜いたタワーが崩れる。本文の「ジェンガのタワーから最下段のブロックを 1 つ抜いたら全体が崩れた」に対応。

初版の `unpublish` 矢印は終点が「2 つのタワーの間の空間」で、0-1 の縦線と同じく行き先が定まらない指定だった。矢印をやめて `before` / `after unpublish` のラベルにしたところ、何が起きたかは崩れ方が語るので情報は失われない。

`your app` の青ベタは白+細枠へ(4 ブロックは同じ依存グラフの層で同格。図 3-2 の規則)。オレンジは left-pad と、それが消えた跡の破線のみ。

**崩れたタワーは生成 AI が苦手とする表現**と予想して代替案(下段が抜けて浮いている状態)も用意していたが、ブロックが互いに食い違って傾く形で破綻なく描けた。

### 図 6-1 yarn v1 が解決した 3 つの課題(2026-08-30 / 標準 3:2)

`docs/history/06-yarn.md` — 遅さ・非決定性・オフラインの 3 課題と、それぞれの解決策を上下で対にする。

上段を light gray から**白+太いオレンジ枠**に変更した。gray だと上段が背景に沈み、「3 つの解決策」だけの図に見えてしまう。オレンジ(問題)→ 青(解決)の対比にすると、yarn が何を解決したかが一目で読める。図 4-1(オレンジ=食い違い)と 4-2(青=一致)と同じ語彙。

下段の青ベタも白+太い青枠へ(図 3-2 の規則。上下は同格で違うのは状態)。

**アイコン 6 個は今回の弱点**と予想していた。特に「かたつむり=遅い」「サイコロ=非決定的」は抽象概念の図像化で、別の絵に置き換わる恐れがある。実際は 6 つとも意図どおり描き分けられた。

生成結果は 1717x916(約 1.87:1)で 3:2 指定よりやや横長。3 列×2 段の構図では収まりが良いのでそのまま採用。

### 図 6-2 yarn v1 と Berry の分岐(2026-08-30 / ワイド 2:1)

`docs/history/06-yarn.md` — 2016 yarn v1 → 2020 Berry で分岐し、上枝が v4 PnP、下枝が v1 メンテナンスモード。

**`many teams` の人アイコンを落とした**。初版は下枝の「上に乗る」オレンジの人アイコンで、線上のどこに置かれるか不定だった(0-1 の浮遊要素と同じ問題)。加えて「多くのチームが v1 に留まる」は事実だがこの図の主題ではない。図の主題は分岐の構造で、それは実線 vs 破線・矢印あり vs なしで既に表現できている。

破線を gray から dark に変更。gray だと背景に沈んで線があること自体が読みにくい。**破線であること自体が「止まっている」を示す**ので、色を薄くする必要はない。

年号は本文と照合済み(2016 誕生、2020 Berry)。ラベルは 5-1 と同じ 2 行形式。年表なのでワイド 2:1。

分岐する線は 0-1 で失敗した「線の起点・終点」の問題を含むが、fork 点から 2 本が出る形は崩れなかった。

### 図 7-1 npm/yarn/pnpm/Bun の系譜図(2026-08-31 / ワイド 2:1)

`docs/history/07-pnpm-and-next-gen.md` — 4 ツールの誕生年をレーンで並べる。

**初版プロンプトの `pnpm 2017` を 2016 に修正した**。本文は「最初のコミットは 2016 年 1 月(v1 リリースは 2017 年 6 月)」で、系譜図は誕生年で揃えるべき。加えて本文が「奇しくも yarn と同じ年に生まれています」と書いているので、2017 のままだと図と本文が矛盾する。**プロンプトの上書きを飛ばすと再生成で 2017 に戻るため要注意**。

`Berry 2020` の fork と `lockfile` 矢印を削除。分岐は 6-2 が専門に扱っており再掲は重複。レーン間を跨ぐ矢印は交差しやすい。

pnpm レーンのみ青。7 章は「本書の立場」を明示する章で、直後に Part III(pnpm の内部構造)へ入る導線上の理由。他 3 つは同じ dark なので優劣を描いてはいない。

**終端処理の追加を試みて失敗し、前版に戻した。**4 レーンの下に共通の時間軸を 1 本引き、右端に矢印と `2026` を置く案を出したが、生成された軸線はどのレーンとも無関係な位置・長さで、5 本目のレーンに見えた。npm(2010)より左から始まるため「2010 より前から存在する何か」とも読める。`2026` も軸の目盛りではなく「2026 に何かが起きた」ように見えた。**指摘した「終端がない」は実害のない些細な点で、修正で入れた要素の方が明確な害を生んだ**。

### 図 7-2 4 ツールのポジショニングマップ(2026-08-31 / 標準 3:2)

`docs/history/07-pnpm-and-next-gen.md` — 横軸=従来型/新方式、縦軸=専業/ランタイム込み。

**pnpm のドットの `(largest)` 指定を外した**。大きさは「優れている」と読まれ、CLAUDE.md の中立性ルールに反する。ポジショニングマップは**位置で語る図**であり、大きさで優劣を付けるのは別の主張になる。色は青のままにしたが、これは 7-1 と同じく「本書がこれから深掘りする対象」を示すもの。

Bun のドットを orange から dark へ。オレンジは警告色で、Bun は問題ではない。

配置指定を `bottom-center-right` のような曖昧な表現から**象限**に変更した。npm と pnpm は両方「専業 × 従来型」で同じ左下、pnpm の方が新方式寄り(右)という関係。曖昧な指定だと pnpm が縦軸のどちら側にいるか決まらない。

象限チャートは軸ラベルが軸線と重なりやすいと予想したが、arrowhead の外側という置き場所を明示したことで崩れなかった。

### 図 8-1 pnpm の導入ルート 3 種(2026-08-31 / 標準 3:2)

`docs/pnpm/08-getting-started.md` — curl script / npm -g / Homebrew の 3 ルートが pnpm CLI に合流。

**初版プロンプトに `gem icon` が残っていた**。0-1 で「pnpm=宝物」として問題にしたのと同じアイコン指定。ここは pnpm 章なので 0-1 ほど深刻ではないが、合流先を宝石で描く必然性はないので削除した。**同じ指定が他の未配置図にも残っている可能性がある**。

`install` の矢印ラベルも削除。3 ルートは対等な選択肢で、1 本だけラベルを付けるとその経路だけが「本物のインストール」に見える。

**1 回目の生成はアイコンがカラーの 3D イラストになり不採用。**青いグラデーションの立方体、オレンジ+泡付きのビールジョッキで、プリセットの `No gradients, no shadows, no 3D` に違反していた。加えてオレンジが装飾として使われ、「問題・注意」という本書の色の語彙が壊れていた。`ICON STYLE:` の節を足して線画・単色・塗りなしを明示したところ解消。

`npm -g` のアイコンは立方体からトレイへの下向き矢印に変更(pnpm CLI と同じ絵になるのを避けるため)。合流先はアイコンなし・青枠のみで十分目立つ。

**`gem icon` は全 33 図から削除済み(2026-08-31)**。0-1(初版)・8-1・8-2・10-3 の 4 箇所にあった。`grep -rn "gem icon" docs` で残存ゼロを確認。

### 図 8-2 pnpm コマンド体系マップ(2026-08-31 / 標準 3:2)

`docs/pnpm/08-getting-started.md` — 中心の pnpm から Dependencies(install/add/remove)と Execute(run/dlx/create)へ。

**残存していた `gem icon` を削除**(前回の調査で判明していた 2 箇所のうち 1 つ目)。中心の塗りも青ベタから白+太い青枠へ。青枠でも中心として十分認識できる。

コンテナの `box icon` / `play icon` も削除した。1 つのコンテナに「タイトル+アイコン+3 つの箱」を詰めると密度が高すぎるうえ、`Dependencies` / `Execute` は文字で明確でアイコンの追加情報がない。

要素 9 個(pnpm + 2 タイトル + 6 コマンド)とやや多いが、左右対称のコンテナ構図(3-1 で成功済み)なので箱は潰れなかった。

残る `gem icon` は `docs/pnpm/10-advantages.md:214`(図 10-2)の 1 箇所。

### 図 9-1 store → hardlink → symlink の 3 層(2026-08-31 / 標準 3:2)

`docs/pnpm/09-how-pnpm-works.md` — グローバルストア → virtual store → ルート node_modules。

**symlink の矢印の向きを逆にした**。初版は `from "node_modules" to ".pnpm"` で、リンクの実装としては正しい(シンボリックリンクの実体は node_modules 側にあり .pnpm を指す)。だがこの図は**3 層の流れ**を示すもので、直前の Mermaid が `ストア → .pnpm → ルート` と左から右へ流れている。図だけ逆向きだと層の順序が読めない。

`hard link` を **`hard link or clone`** に。CLAUDE.md の正確性ルール(macOS/APFS では CoW クローン)を図にも反映する。

`Project` コンテナ(中に .pnpm と node_modules を入れ子にする)をやめ、横一列の 3 要素にした。本文が「3 層」と言っているのに 2 つが 1 箱に入っていると 2 層に見える。

各要素のラベルを 2 行にして役割(`one per machine` / `virtual store` / `what you declared`)を併記。図だけで 3 層の違いが分かるようにする。

**1 回目の生成は `node_modules/..pnpm` とドットが 2 つになり不採用。**ピリオドの個数を明示する指定で解消(教訓に一般化済み)。

### 図 9-4 複数プロジェクトが 1 つの store を共有(2026-08-31 / 標準 3:2)

`docs/pnpm/09-how-pnpm-works.md` — グローバルストア 1 つを 3 プロジェクトが共有。

**円柱を 9-1 の Global Store と同じ描き方に統一した**(白+太い青枠、アイコンなし)。初版は青ベタ+DB アイコンで、同じ「グローバルストア」が同一章内で違う絵になると読者が別物かと迷う。章をまたぐ一貫性より**同一章内で同じものを同じ絵にする**方を優先。

配置を放射状(中心の周りに 3 つ)から**左に 1・右に縦 3** へ変更。放射状は 3 本の矢印の長さと角度がばらつく。8-1 で成功した 3 対 1 の構図を左右反転した形。

`hard link` を `hard link or clone` に(9-1 と同じ正確性ルール)。

矢印ラベルは 1 本だけ。8-1 では「3 つの対等な選択肢」なので 1 本だけラベルを付けると不公平だったが、**ここは同じ関係が 3 本ある**ので代表 1 本で足りる。3 本すべてに書くと図が文字で埋まる。

円柱のラベルに `one copy on disk` を追加。ディスク上の実体が 1 つであることがこの図の主張そのもの。

### 図 9-2 .pnpm 内のレイアウト詳細(2026-08-31 / 標準 3:2)

`docs/pnpm/09-how-pnpm-works.md` — 公式の最小例(foo@1.0.0 が bar@1.0.0 に依存)を図にしたもの。

**9 章で最も要素が多い図**。入れ子が 2 段(コンテナ → `foo@1.0.0` → `foo`)、矢印 3 本、うち 1 本は入れ子の内側同士を結ぶ。本文の最小例をそのまま図にする必要があるので簡略化はできない。

一番の懸念は symlink の矢印が**外側の箱同士**を結んでしまうことだった。本文は「`foo@1.0.0/node_modules/bar` が `bar@1.0.0/node_modules/bar` を指す」と説明しているので、内側同士でないと不正確になる。`from the inner box "foo" ... to the inner box "bar"` と内側であることを明示して通った。

`.pnpm` のドットは 9-1 と同じく個数を明示。円柱も 9-1・9-4 と同じ描き方に統一(同一章で 3 回目の登場)。`hard link` → `hard link or clone`。

### 図 9-3 npm の flat と pnpm の strict の対比(2026-08-31 / 標準 3:2)

`docs/pnpm/09-how-pnpm-works.md` — 3 章の幽霊依存の伏線回収。同じ import が npm では通り pnpm では失敗する。

**`your code` を上部に 1 つ置く構成をやめ、各パネルに 1 つずつ複製した**。初版は上に 1 つ置いて左右へ矢印を伸ばす形で、パネルをまたぐ矢印が交差する(7-1 でレーン間の矢印を落としたのと同じ問題)。左右を独立させると「同じ状況で結果だけが違う」対比が読める。

`linked` / `hidden` と左右で違っていた語を、両方 `declared` に統一(3-1 の教訓。同じものが違う名前だと別物に見える)。

`blocked` を **`not found`** に変更。本文は「Node.js の解決アルゴリズムの時点で失敗」と書いており、実際に出るのは `Cannot find module`。動作の説明より実際の結果を書く。

`hidden` の枠を破線に。`.pnpm` の中には存在するがルートからは見えない状態を示す(5-2 で消えたブロックを破線で表したのと同じ語彙)。

### 図 10-1 store 共有によるディスク節約(2026-08-31 / 標準 3:2)

`docs/pnpm/10-advantages.md` — npm は各プロジェクトが実体を抱え、pnpm はストアを共有する対比。

**左右で `Project A/B/C` を共通の名前にした**。同じ 3 プロジェクトが配置方式によってどう変わるかを示す図なので、名前が一致していないと別物の比較に見える(3-1 の教訓)。

初版の `full copies` グループキャプションを削除し、各プロジェクト箱の中に `files`(オレンジ枠)を置いた。浮遊キャプションは位置が不安定(0-1 の失敗)。同じファイルが 3 箇所に重複している状態を箱の中で示す方が主張に直結する。

**同じ文字列が 6 回登場する図**(`Project A/B/C` が左右で各 2 回)。9-3 の 2 回は通っていたが 6 回は初めてで、名前の入れ替わりや欠落を懸念した。順序どおり正しく描かれた。

円柱は 9-1・9-2・9-4 と同じ描き方(白+太い青枠、アイコンなし)。数値は図に入れない規則があるので `3.7M` などは使っていない。

### 図 10-2 セキュリティ多層防御(2026-08-31 / ワイド 2:1)

`docs/pnpm/10-advantages.md` — Registry から 4 つの防御機構を通って Your Project へ。

**矢印を要素の下に逃がした**。初版は「Registry から 4 つのゲートを通り抜けて Your Project へ」で、矢印が縦長のシールドを貫通してラベルと衝突する。列の下に沿わせても「4 つの関門を順に通過する」流れは同じように読め、衝突は起きない。

横一列 6 要素なのでワイド 2:1(1-2 と同じ判断)。シールド形状は本書で初めてだったが、4 つとも同一の形で描かれた。

指定していないアイコン(時計・立方体・チェックリスト・虫眼鏡)が各シールドに入ったが、`ICON STYLE` の指定どおり線画で統一されており、各機構の意味を補強しているのでそのまま採用。

**`gem icon` の残りは図 10-3**(`docs/pnpm/10-advantages.md:229`)。10-2 だと記録していたのは誤りで、実際は 10-3 の中心円だった。

### 図 10-3 7 つのアドバンテージの俯瞰マップ(2026-08-31 / 標準 3:2)

`docs/pnpm/10-advantages.md` — 中心の pnpm から 7 つのアドバンテージへ。10 章のまとめ。

**初版プロンプトは 7 つのうち 6 つしか列挙していなかった**(Disk / Speed / Strictness / Monorepo / Security / Compatibility)。本文の「⑦ 勢い」に対応する項目がなかったので `Momentum` を追加。プロンプトと本文の項目数が食い違っていた例。

`gem icon` を削除し中心円を白+太い青枠に(8-2 の中心円と同じ扱い)。**これで全 33 図から gem icon が消えた**。

spokes は矢印なしの直線と明示した。矢印だと「pnpm が生み出す」という因果に見えるが、ここは分類なので線で十分。

**本書で要素数が最多の図**(中心 1 + リング 7 + 線 7)。放射状は 9-4 で一度失敗して構図を変えた形式であり、7 要素の等間隔配置は崩れる懸念があったが、ハブ&スポークとしては素直に描かれた。

### 図 11-1 モノレポの全体構造(2026-08-31 / 標準 3:2)

`docs/pnpm/11-workspaces.md` — monorepo root に pnpm-workspace.yaml / pnpm-lock.yaml と apps/packages。

**`pnpm-lock.yaml` のオレンジをやめ、青に統一した**。本書はオレンジを一貫して「問題・注意」に使っている(3-3 の未宣言、4-1 の食い違い、6-1 の課題、10-1 の重複)。lockfile は 4 章で「再現性をもたらす良いもの」として扱っており、警告色は不適切。

矢印は 1 本ずつ別の箱へ向かわせた。初版の「2 つの箱の方へ」という曖昧な指定は 0-1 で失敗した「1 本が複数に分岐する」構図になる。2 ファイルがワークスペース全体を支えることは、同じコンテナ内に同居している事実で伝わる。

**ファイル名が長いのが懸念だった**(`pnpm-workspace.yaml` は 19 文字でプリセットの 1-3 words を大きく超える)。9-1 で `node_modules/.pnpm` が化けた前例があるので綴りを重点確認したが、両方とも正確に描かれた。
